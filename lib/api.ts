// Shared API types for client and server communication

export interface DemoResponse {
  message: string;
}

export interface PingResponse {
  message: string;
}

// Add other shared API types here as needed
